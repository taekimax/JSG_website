# JSG 디자인 실험 worktree — 작업 지침

### 글꼴·모바일 Partners — 2026-09-10 최신 결정

- 사용자 의도: 본문까지 코딩용 고정폭 글꼴을 사용해 테크니컬한 인상을 준다. 현재 영문 JetBrains Mono·한글 D2Coding을 유지한다. 아래의 이전 Pretendard 본문 규칙은 이 결정으로 대체한다.
- 모바일 Partners는 여러 인물을 함께 보여주는 의도를 유지하며 약 1.5장으로 확대한다. 카드 최소 폭은 220px이며, 더 좁은 화면에서는 보이는 장수가 줄어든다. 이름은 22px, 직함·경력은 16px, 사진 원본 비율과 원문을 유지한다.
- 모바일 폭(699px 이하)에서 페이지를 열면 Partners는 기본 정지하며 사용자가 재생할 수 있다. 데스크톱과 Portfolio의 기존 자동 이동 설정은 유지한다.
- Team 본문의 Partners 소제목과 그 아래 회색 구분선은 제거한다. 재생·정지 버튼은 Team 제목 바로 오른쪽에 공통 24px 간격·44px 조작 영역으로 배치한다. Advisors 소제목과 상세의 인물 그룹 표시는 유지한다. 아래의 Partners 소제목 옆 버튼 규칙은 이 결정으로 대체한다.
- 사진 아래 경력 요약은 기존 원문의 ` · ` 구분과 줄바꿈을 기준으로 항목별 문단으로 표시한다. 요약의 원문·항목 순서를 유지하며, 홈 카드와 멤버 상세에 동일하게 적용한다.
- 이 결정은 로컬 개선 범위다. 현재 초안을 최종 디자인으로 간주하지 않으며 커밋·배포는 별도 요청을 따른다.

### Team 최신 변경 — 2026-09-10

- 사용자 요청으로 이도훈 자문위원을 김영우 박사로 교체했다. 사용자가 제공한 `팀_사진/김영우 교수 약력.docx`의 직함·경력을 사용하며, 상세 ID는 `kim-young-woo`다. 이전 인물의 경력·출처 메타데이터는 승계하지 않는다.
- 제공된 자문위원 사진 9개를 반영했다. 김명천은 기존 사진을 유지하며, 사진이 채워진 자문위원에는 이전 placeholder 규칙을 적용하지 않는다. BMP 원본은 표시용 PNG로만 변환했다.
- 이종기·박근령·하지혜를 웹사이트 멤버 목록에서 제거했다. Partners 순서는 박상진 → 김석범 → 최승만 → 윤영진 → 김태형 → 박준혁이다. 목록과 상세 이전/다음은 동일한 manifest 정렬을 사용한다.
- PASS: `npm run verify` 33개 및 자산 검사, 데스크톱·390px 목록, 자문위원 이미지 로딩, 김영우 상세 약력, 김석범 앞뒤 탐색, 제거된 네 인물의 상세 미존재 표시. 이번 변경은 로컬 미리보기에 반영했으며 커밋·공개 배포는 하지 않았다.

## 목적과 적용 범위

이 worktree는 기존 JSG 사이트 위에서 현대적인 디자인과 사용 경험을 탐색하는 독립 실험 공간이다. 이전 프로젝트 지침과 과거 디자인 승인 기록을 이번 작업의 요구사항으로 승계하지 않는다. 현재 사용자 요청과 이번 대화에서 합의한 결정이 작업 기준이다.

- 현재 작업 경로: `/Volumes/dev/JSG_website`
- 현재 브랜치: `main`; 이전 실험 브랜치: `design/modernization-20260906` (역사 기록)
- 생성 기준: `2261da0b70dad287a1c37f746618014debcaba99`
- 원본: `/Users/tk/dev/JSG-website` — 파일, 인덱스, 체크아웃 상태를 변경하지 않는다.
- 모든 편집, 의존성 설치, 빌드, 미리보기는 실험 worktree를 기준으로 수행한다. 명령 실행 전 작업 경로를 확인한다.

## 이번 요청으로 유지하는 콘텐츠 계약

다음은 이전 디자인을 보존하기 위한 제한이 아니라 사용자가 이번 작업에서 명시한 보존 범위다. 세부 형식은 `ASSETS_CONTRACT.md`와 현재 데이터·렌더러에서 확인한다.

- 기존 콘텐츠 파이프라인과 approved copy는 보존한다. 이번 요청으로 승인된 예외는 Partners의 기존 영문 요약을 바탕으로 한 한글 요약 추가와 Philosophy 외부 글 목록용 JSON 확장, Contact 주소·연락처 교정과 위치 약도 교체, 2026-09-07 승인된 별도 Notice 콘텐츠 저장소와 정적 HTML 출력이다. 그 밖의 기존 한글·영문 원문을 유지한다. 디자인을 위해 문구를 임의로 줄이거나 새 번역·사업 정보를 만들지 않는다.
- 웹사이트 콘텐츠 관리자는 `assets/**`에서 텍스트, 이미지와 manifest를 관리한다. Notice 관리자는 Pages CMS를 통해 별도 비공개 `taekimax/jsg-board-content` 저장소만 편집한다. HTML·CSS·스크립트 편집을 관리자에게 요구하지 않는다.
- 페이지별 manifest 진입점, 스키마, 경로 규칙, 안정적인 ID, 정렬, `assetVersion` 캐시 갱신을 유지한다.
- 기존 콘텐츠 hydration 연결점과 의미를 보존한다. DOM을 재구성할 때 데이터가 들어갈 대상, 언어 연결, 문단 및 목록 렌더링이 유지되어야 한다.
- 팀·포트폴리오·공지의 상세 URL과 ID 연결, 공지 본문·첨부·이전/다음 탐색을 유지한다.
- 위에 명시된 사용자 승인 예외 외에는 콘텐츠 파이프라인과 approved copy 변경을 구현 범위에 넣지 않는다. 디자인은 기존 계약 위에서 해결한다. 충돌이 발견되면 설명하고, 계약을 조용히 바꾸지 않는다.

## 다시 결정할 디자인

승인된 About의 차분한 구성을 기준으로 발전시킨다. 아직 검토하지 않은 개별 페이지의 카드·이미지·본문 구성은 후속 사용자 결정에 따라 재설계할 수 있다. 과거 디자인의 세부사항을 새로운 제약으로 승계하지 않는다.

### 현재 합의한 결정 — 2026-09-06

- 결정: 우선 방문자는 투자받을 창업자와 JSG의 LP 투자자다. 두 대상 사이의 상대적 우선순위는 아직 정하지 않았다.
- 결정: 브랜드는 ‘이성에 근거한 대담한 비전’을 전달한다. 실용적이고 현실적인 디자인을 선호하며 금융·투자회사 정체성을 유지한다.
- 이유: 사용자가 두 방문자 집단과 브랜드 방향을 직접 지정했다. ‘대담함’만으로 과도한 시각 효과나 시네마틱 방향을 정당화하지 않는다.
- 적용 범위: About 홈과 내부 페이지 전체의 디자인 탐색·비교·구현 판단.
- 결정: 사용자가 About 시안 A의 차분한 구성을 승인했다. 독립 랜딩을 제거하고 About을 첫 페이지로 사용한다. `index.html`과 이전 `landing.html` 주소는 `about.html`로 직접 연결한다.
- 결정: 밝은 기본 화면과 승인된 About의 타이포그래피·여백을 발전시킨다. 상세 화면은 공통 타이포그래피·색면으로 정리하며 추가 장식 그래픽은 사용하지 않는다.
- 결정: Index Ventures를 주요 레퍼런스로 삼아 전체 구성을 가능한 범위에서 가깝게 참고한다. 기존 사이트는 배포 전 초안이며 시각적 편집은 자유롭다. JSG의 콘텐츠 규모에 맞춰 재구성하고 레퍼런스의 고유 로고·카피·사진을 가져오지 않는다.
- 결정: 이미지 사용을 최소화한다. 현재 자산을 임시로 활용하고 추후 적절한 이미지로 교체한다. 무료 이미지 소스는 필요할 때 추천한다.
- 결정: 내비게이션 아일랜드와 하단 시트를 삭제한다. About → Team → Philosophy → Portfolio → Notice → Contact의 긴 단일 페이지로 통합한다. 상단 로고·메뉴 바는 고정하지 않고 문서와 함께 스크롤한다. 메뉴는 해당 섹션으로 24px 여백을 두고 이동하며 현재 위치를 표시한다. 하단 Top은 문서 맨 위로 돌아간다. 모바일에서는 펼침 메뉴를 사용한다.
- 결정: 회사 로고를 화면에서 제거하고 홈에 회사명이 흐르는 목록을 둔다. 정지·키보드 탐색·동작 줄이기를 지원한다. 기존 portfolio manifest가 이름·정렬·상세 연결을 계속 소유한다. 회사 목록에는 고정 개수 제한을 두지 않는다.
- 결정: 멤버 사진은 Team 섹션과 멤버 상세에 집중한다. 일부 섹션에 강한 색면을 허용하며 팔레트 통일성을 유지한다. Philosophy는 기존 RoyalBlue, Contact는 남색을 유지한다. 추가 그래픽은 사용하지 않기로 확정했다. `docs/design/graphic-direction-handoff.md`는 이 결정 기록이며 이전 탐색 요청을 재개하는 지시가 아니다.
- 결정: 사용자가 제공한 새 도형을 회사 로고로 지정했다. 회사명은 `JSG INVESTMENT`이며 두 단어는 같은 글자 크기다. 도형 위·텍스트 아래 배치를 기본으로 하고 텍스트 전체 폭은 실제 도형 폭의 두 배 이하다. 웹에서는 도형+HTML 텍스트, 문서용으로는 회사명 포함 PNG를 제공한다.
- 로고 자산: `assets/shared/jsg-emblem.png`, `assets/shared/jsg-logo-lockup.png`. 제공 PNG의 체크무늬를 이미지 도구로 정리한 흰 배경 버전이며 투명 파일이 아니다. 실패한 투명 시안은 사용하지 않는다. 기존 공통 manifest의 `logo`와 `assetVersion`만 갱신했다.
- 보존 계약: 명시된 승인 예외 외에 콘텐츠 파이프라인과 approved copy를 보존한다. 기존 랜딩의 회사 소개 원문은 홈 하단에도 존재하며, `assets/landing/` 파일 자체도 유지한다. 사라지는 입장 버튼은 랜딩 제거 결정에 따른 UI 변경이다.

- 결정: 메뉴·영문 섹션 제목·포트폴리오 회사명은 고정폭 JetBrainsMono Nerd Font Regular를 사용한다. Propo가 아니다. 한글·영문 본문과 회사 로고 문자는 Pretendard이며 로고는 SemiBold다. 폰트 파일과 라이선스를 `assets/fonts/`에 포함해 직접 제공한다. 한글 제목은 포함된 Pretendard로 대체 표시한다.
- 결정: 메뉴·섹션 이름은 About / Team / Philosophy / Portfolio / Notice / Contact로 간결하게 유지한다. UI 소제목은 Partners / Advisors를 사용한다. 이는 현재 사용자가 요청한 제목 표현 변경이며 사업 소개 원문은 유지한다.
- 결정: 일반 본문은 데스크톱 18px, 모바일 17px 중심으로 확대한다. 사이트 맨 아래로 옮긴 `ABOUT JSG INVESTMENT` 소개 블록은 label 12px·본문 14px·로고 20px를 유지한다.
- 결정: About·Portfolio 제목 영역만 로고에서 추출한 공통 Cyan `#04f8fd`와 DeepBlue 글자, Team 제목 영역만 Philosophy와 동일한 RoyalBlue `#1d4ed8`와 흰 글자를 사용한다. 홈 Notice 제목과 목록은 파란 배경·흰 글자로 표시한다. Notice 상세는 파란 제목과 흰 본문을 사용한다. Team 본문·Portfolio 회사명 영역은 흰색이다. Philosophy 전체 파랑과 Contact 전체 남색은 유지한다. 팔레트 값은 `styles/tokens.css`에서 관리한다.
- 공통 팔레트: Cyan·Blue·Navy·Yellow·중요 표시 Red·White에 회색 3단계(`#f4f4f4` 배경, `#d4d4d4` 구분선, `#656565` 보조 글자)와 Black(`#000000` 본문)을 사용한다. UI의 실제 색상 값은 `styles/tokens.css`에만 두고 그라데이션·투명도도 같은 팔레트에서 파생한다.
- 게시판 최신 결정 (2026-09-07): 사용자가 Pages CMS 브라우저 편집 → 비공개 `jsg-board-content` 저장소의 Markdown → 정적 출력 방향을 승인했다. 웹사이트는 `/board-content/`의 생성 파일만 읽으며 공지 저장으로 이 저장소를 변경하지 않는다. Cafe24 SFTP 연결은 호스팅 준비 후 활성화한다.

### 상세 화면·콘텐츠 표시의 최신 결정 — 2026-09-07

- Team·Portfolio·Notice 상세는 `styles/detail.css`의 공통 구조를 사용한다. 기존 상단 장식 사진은 제거하고 멤버 인물 사진은 유지한다.
- Partners는 연회색 카드 상단에 원본 비율의 전체 사진과 이름·직함을 나란히 두고 아래에 한글·영문 요약, 가로 자동 반복 목록을 사용한다. 터치·휠·키보드 탐색은 자동 이동을 멈추며 재생/일시정지와 동작 줄이기를 지원한다. Partners 영문 직함의 `/`는 카드에서 줄바꿈으로 표시한다. Advisors는 요약을 표시하지 않는다.
- Partners·Advisors 제목은 Philosophy의 Respect·Health·Integrity와 같은 글꼴·크기·스타일이다.
- About 한영 제목은 각각 두 줄로 명시적으로 나누고 같은 줄 높이·행 배치를 사용한다. Team·Philosophy 설명은 콘텐츠 폭 전체를 사용한다. Notice·Contact 제목 아래 설명은 후속 요청으로 제거했다.
- `ABOUT JSG INVESTMENT` 소개를 홈 하단 footer로 옮겨 합쳤다. 글자 크기와 기존 콘텐츠 소유자는 유지한다.
- Philosophy의 외부 글은 Substack 한 채널과 무료 Datawrapper 표 사용으로 결정했다. 별도 콘텐츠 저장소가 제목·날짜·링크와 RSS 동기화 도구를 소유하며 브라우저는 `/board-content/perspective-manifest.json`을 읽는다.
- Contact는 전체 폭의 전화·이메일·한영 주소와 그 아래 지도 순서로 표시한다. Location 및 중복 Contact 소제목은 제거했다. 라벨과 값을 가로로 배치하고 같은 크기(데스크톱 20px, 모바일 17px)를 사용한다. 좁은 화면에서 긴 주소는 자연스럽게 줄바꿈한다. 영문 주소는 `Samheung2 Bd 8 Fl., Tehran 514, Gangnam, Seoul`이며 기존 manifest에 연결한 `location-address-en.txt`가 소유한다. 주소는 `서울특별시 강남구 테헤란로 514, 삼흥2빌딩 8층`, 전화는 `02-3567-7751`, 이메일은 `contact@jsginvest.com`이다. 기존 manifest/TXT 소유자를 통해 표시한다.
- 사용자가 요청한 기능성 위치 약도는 장식 그래픽 제외 결정의 예외다. `assets/contact/contact-map.svg`와 PNG를 800×600으로 유지한다. 데스크톱에서는 연락처 기준선에 맞춘 최대 572×400px의 10:7 표시 영역으로 높이를 줄이고 모바일에서는 기존 10:7 화면 높이를 유지한다. 테헤란로·영동대로와 JSG·현대백화점·파르나스·삼성역·코엑스·현대차·도심공항터미널·섬유센터·글라스타워·파크 하얏트 서울만 표시한다. 건물 박스와 JSG 연결선은 없으며 흰 도로가 좌측 사이안·우측 블루 사각 색면을 나누는 현대미술 카드 형태의 약도다. 삼성역 교차점은 오른쪽 끝에서 가로 1/3 위치다. 글라스타워는 교차점 남서쪽, 파크 하얏트 서울은 남동쪽이다. 포스코센터는 사용자 요청으로 제외했다. JSG는 왼쪽으로 옮기고 고정폭 JetBrainsMono 52px의 `JSG`를 Navy로 수평 표시한다. 이 표식만 흰 배경·Navy 테두리·Contact와 같은 Navy의 번짐 없는 오른쪽 아래 그림자를 가진 네오브루탈리즘 버튼 모양으로 강조한다. 실제 클릭 버튼은 아니다. 다른 이름은 28px로 도로에 맞춰 기울였으며 현대차는 영동대로 오른쪽 코엑스 맞은편에 같은 기울기로 표시한다. 섬유센터·글라스타워는 테헤란로 가장자리와 여백을 두고 배치했다. 거리는 단순화했다.
- Notice는 설명을 제거하고 제목·목록 및 항목 사이의 여백을 줄였다. 중요 배지는 흰 바탕·빨간 글자로 유지한다.
- Perspective와 Notice의 게시물 제목은 같은 반응형 크기 규칙(15–24px)을 사용한다. 개별 공지 예외 없이 목록 전체에 적용하며, 320px 이상의 화면에서도 2026년 여신전문금융업 통일경영공시 제목이 한 줄을 유지할 수 있는 크기로 표시한다.
- 미결: 실제 Substack URL·첫 공개 글 검증, 이전 실서비스 게시판 원본 이관, Cafe24 SFTP 변수·비밀값 설정.

### 게시판 탐색 — 2026-09-07

- 결정: Perspective와 Notice는 기존 세로 목록을 최대 5개씩 묶어 가로로 넘긴다. 제목 영역에 이전·페이지 수·다음 순서로 두며, 태블릿·데스크톱에서는 페이지 제목과 같은 줄을 유지한다. 좁은 모바일에서 실제로 들어가지 않을 때만 제목 바로 아래의 같은 왼쪽 기준선에 둔다.
- 이유·적용 범위: 사용자가 모바일 제목 영역의 가로 탐색 컨트롤을 데스크톱에도 동일하게 적용하기로 했다. 수평 스와이프·트랙패드·버튼·좌우 키로 페이지를 이동하며 문서의 세로 스크롤을 유지한다.
- 표시·동작: 페이지 단위 스냅, 44px 버튼 영역, 현재/전체 페이지 표시, 양 끝의 비활성 화살표를 사용한다. 자동 재생은 없고 동작 줄이기를 지원한다. 각 게시판의 페이지는 탭 세션에서 독립적으로 기억한다.
- 탐색 버튼은 모든 화면의 제목 영역에서 공통 JetBrains Mono 왼쪽·오른쪽 chevron만 가로로 표시하고 가운데에 현재/전체 페이지 수를 둔다. Notice의 두 버튼은 흰색, Perspective의 두 버튼은 DeepBlue로 통일하며 비활성 상태도 같은 색을 유지한다. 버튼은 접근 가능한 Previous/Next 이름과 최소 44px 터치 영역을 유지한다.
- 탐색 그룹은 페이지 제목 바로 옆의 읽기 흐름에 배치한다. 태블릿·데스크톱에서는 제목과 같은 줄을 유지하고, 좁은 화면에서 줄바꿈이 필요하면 제목 바로 아래 왼쪽에 둔다.
- 보존 계약: `scripts/boardPager.js`는 기존 행을 그대로 묶는다. 원문·정렬·ID·상세 링크·첨부를 유지한다. Notice와 Perspective 데이터는 `/board-content/`의 생성 manifest를 통해 로드한다.

과거의 ‘시네마틱·창업자 중심’, 어두운 외곽과 밝은 본문, 한영 동등 강조는 이번 작업의 확정 방향이 아니다. 과거의 기술 스택 고정 규칙 역시 승계하지 않는다. 현재 기술 구성은 출발점이며, 큰 기술 변경은 목적과 콘텐츠 계약에 미칠 영향을 설명하고 판단한다.


### 분리된 Markdown 게시판 — 2026-09-07 최신 결정

- 결정 / 이유: 가끔 작성하는 관리자 공지는 Pages CMS와 별도 비공개 콘텐츠 저장소로 관리한다. 사용자는 서버·Docker 운영을 하지 않는다.
- 적용 범위: `/Volumes/dev/jsg-board-content`가 Markdown, 첨부, Perspective RSS 데이터, 생성기와 배포 워크플로를 소유한다. 이 웹사이트 저장소는 `assets/shared/board-endpoints.json`과 표시 코드만 소유한다.
- 보존 계약: 기존 5개 공지 원문·ID·날짜·첨부를 이관했다. 날짜는 작성자가 지정한 `YYYY-MM-DD`를 유지한다. 새 ID는 자동 생성한 고정 파일명에서 얻고 `draft: true`로 게시를 내린다. Markdown raw HTML은 실행되지 않는다.
- 기본 편집기: `/admin/`이 `taekimax/jsg-board-content`의 hosted Pages CMS로 이동한다. GitHub App 범위는 이 비공개 저장소 하나로 제한한다.
- 자동화: 콘텐츠 저장소의 GitHub Actions가 검사, 출력 artifact와 Perspective 예약 동기화를 정의한다. Cafe24 배포는 명시적인 활성화 변수와 SFTP 비밀값을 넣기 전까지 실행되지 않는다.
- Mock: 콘텐츠 저장소에 Notice 7개와 Perspective 8개를 preview fixture로 보관해 각 12개·3페이지(5/5/2)를 확인한다.

## 대화형 디자인 절차

1. 유지할 계약, 사용자 결정 사항, 조사 사항, 구현자 자율 사항을 구분한다.
2. 관련된 핵심 질문을 작은 묶음으로 제시한다. 각 선택지의 장단점과 실제 화면에 미칠 영향을 설명한다.
3. 중요한 디자인 방향은 사용자 답변 전에 확정하지 않는다. 미확정 방향을 전제로 대규모 소스 변경을 시작하지 않는다.
4. 조사 범위가 넓으면 먼저 범위를 제안한다. 레퍼런스는 구성 원칙을 비교하는 자료로 사용한다.
5. 결정은 대화에서 `결정 / 이유 / 적용 범위 / 보존 계약·미결 사항`으로 짧게 남긴다. 결정 여부를 명확히 구분한다.
6. `DESIGN.md`가 필요하면 구조와 내용 초안을 대화에서 검토한 뒤 작성한다. 콘텐츠 계약이나 이 지침을 대신하는 문서로 사용하지 않는다.
7. 방향 합의 후 작은 구현 단위별로 목적, 영향 파일, 보존 계약, 검증 방법을 제시한다.
8. 합의된 범위의 간격, 반응형 수치, CSS 구성 같은 세부사항은 구현자가 판단한다. 핵심 방향이 달라지면 다시 논의한다.

외부 플러그인·도구는 자동 설치하지 않는다. 필요성과 영향 범위를 먼저 설명한다. 과거 계획에 적힌 스킬·에이전트·커밋 절차는 현재 지시가 아니다.

## 현재 코드 지도 — 제약이 아닌 관찰

- 주요 섹션은 `about.html`에 통합했다. 기존 Team·Philosophy·Contact 주소는 홈 앵커로 연결한다. Portfolio·Notice도 ID가 없으면 홈으로 연결하며 상세 URL은 유지한다. 멤버 상세는 `team-member.html?id=`를 유지한다.
- 홈: `index.html` → `about.html`. `landing.html`은 이전 주소의 리다이렉트만 남아 있다.
- 랜딩 전용 `src/`, 생성 번들, React·Three·Vite 의존성과 빌드 설정은 제거했다. 현재 런타임은 정적 HTML·CSS·일반 JavaScript다.
- 공유 내비게이션: `scripts/ui.js`, `styles/layout.css`.
- 회사명 목록과 포트폴리오 목록·상세의 단일 렌더러: `scripts/portfolio.js`.
- 콘텐츠: `assets/{page}/`, 공통 자산은 `assets/shared/`.
- 검증: `tools/*.test.mjs`, `tools/validate-assets.mjs`.
- 검증 명령: `npm run verify`. 방문자용 앱 번들이나 게시판 생성 단계는 이 저장소에 없다.

## 검증과 보고

- 콘텐츠 계약 검사와 특정 디자인의 클래스·배치를 검사하는 항목을 구별한다. 테스트 이름만으로 정책을 추론하지 않는다.
- 승인된 디자인이 표현 구조를 바꾸면 관련 검사도 의미에 맞게 조정할 수 있다. 실패를 숨기기 위해 콘텐츠 계약 검사를 약화하지 않는다.
- 문서 변경은 diff와 참조를 확인한다. 동작 변경은 실제 사용 경로와 관련 계약을 확인한다.
- 브라우저 검증은 HTTP 미리보기에서 데스크톱과 모바일을 확인한다. 데이터 로딩, 상세 이동, 첨부, 탐색, 키보드 사용, 가독성, 넘침을 변경 범위에 맞게 살핀다.
- 보고는 실제 디자인·동작 변경, 계약 보존, 구조 테스트·빌드, 실제 브라우저 확인, 사람의 시각 판단, 미결 사항을 구분한다. PASS / FAIL / NOT RUN을 사용한다.
- 테스트·빌드 통과를 시각적 완성도나 사용자 승인으로 표현하지 않는다.

## 현재 구현과 검증 — 2026-09-07

- 각 홈 렌더러는 성공·실패 후 준비 상태를 알린다. 초기 앵커는 콘텐츠가 채워진 뒤 한 번 확정하며 사용자 입력 시 초기 이동을 취소한다. 관리자 콘텐츠 형식과 분리된 표현 계층 동작이다.
- PASS: 웹사이트 Node 구조·렌더러 검사 23개와 자산 검사. 별도 콘텐츠 저장소 Node 검사 7개와 RSS fixture 검사 9개.
- PASS: 실제 Chrome의 320·390·768·1440px에서 최신 제목 배경, 가로 넘침, 하단 회사 소개 크기, Partners 한영 요약·자동 반복·정지, 상세 페이지를 확인했다. 동작 줄이기와 키보드 상세 이동도 확인했다.
- PASS: 홈과 상세의 이전 장식 사진 미요청, About 두 줄 정렬, 소제목 스타일 일치, 전체 폭 설명, 실제 웹 폰트 사용, 모바일 메뉴와 섹션 뒤로 가기.
- PASS: 로컬 `/board-content/` mount에서 Notice와 Perspective 각 12개·3페이지, Markdown 상세, 이미지와 첨부 경로를 실제 Chrome으로 확인했다. 390px 가로 넘침이 없다.
- NOT RUN: 실제 Substack 채널/RSS 연결, Pages CMS 저장, Cafe24 업로드, 모든 물리적 기기·브라우저 조합, 사용자 최종 시각 승인. 별도 사이트 빌드 단계는 없다.

## 문서의 역할

- `AGENTS.md`: 이번 실험 worktree의 작업 방식과 범위.
- `README.md`: 현재 작업 공간 안내, 실행 방법, 관리자 편집 안내.
- `ASSETS_CONTRACT.md`: 이번 요청에서 유지한 콘텐츠 계약의 상세 형식.
- `docs/superpowers/`, `docs/reviews/`: 이전 작업의 역사 자료. 승인·완료·필수 절차라는 표현은 당시 맥락이며 현재 권한이나 검증 결과가 아니다.


### Portfolio 콘텐츠·상세 탐색 — 2026-09-07

- 사용자 승인: 제공 회사소개 PPT의 기업 목록을 기준으로 공식 웹 자료를 조사해 소개를 갱신한다. 펀드 자체는 제외하고 펀드를 통해 투자한 기업은 포함한다. 샘플 기업 4개를 제거하고 18개 기업을 표시한다.
- 회사 상세는 이름·분야·짧은 사업 소개만 표시한다. 펀드 이름·규모·조건, 투자 세부정보, 출처 링크, PPT 이미지는 표시하지 않는다. 기존 manifest와 회사별 TXT가 콘텐츠를 소유한다.
- Team·Portfolio·Notice 상세는 공통 `detail-pager`의 이전/다음 링크와 footer의 섹션 복귀 링크를 사용한다. 본문 안의 중복 섹션 버튼은 제거한다.

### 사진·상장 정보·구분선 — 2026-09-08

- 사용자 요청으로 About 본문 아래의 불필요한 회색 구분선을 제거했다. 다른 섹션의 목록·탐색 구분선은 해당 기능을 유지한다.
- 제공된 팀 사진 중 1260×1620 상반신 버전만 사용한다. 최승만·윤영진·김태형·박준혁·김석범의 새 JPG는 `assets/team/`에 있으며 `imageLayout: "torso"`로 표시한다. 홈 카드는 데스크톱·모바일 모두 큰 사진 위·이름과 소개 아래 구성이다. 상세 사진도 원본 비율을 유지한다.
- 새 사진이 없는 멤버는 기존 작은 사진을 사용하는 compact 카드 또는 사진 공간 없는 text 카드를 사용한다. 기존 이름·직함·소개·정렬·상세 ID는 유지한다.
- 사용자 요청으로 확인된 상장사의 회사별 TXT 맨 아래에 코스닥·종목코드·상장일을 추가했다. 지투지바이오·그래피·아크릴·알지노믹스·삼보모터스가 해당하며 삼보모터스는 합병 상장으로 구분한다. 근거는 `docs/research/portfolio-listing-2026-09-08.md`에 있으며 공개 화면에는 출처 링크를 추가하지 않는다.

### Portfolio CMS와 GitHub Pages 출력 — 2026-09-08

- 사용자 승인: Portfolio 이름 옆에 KOSDAQ·KOSPI 배지를 표시하되 기존 줄 높이는 유지한다. 320·390·768·1440px에서 추가 전후 줄 높이가 같음을 확인했다.
- Portfolio의 18개 회사 원문·ID·정렬을 `/Volumes/dev/jsg-board-content/content/portfolio/`로 이관하고 Pages CMS에 회사 편집 화면을 추가했다. 기존 사이트의 Portfolio manifest·TXT 소유 규칙을 이 결정으로 대체한다. 웹사이트는 `assets/shared/board-endpoints.json`의 `portfolioManifest`와 생성된 TXT만 읽는다.
- Notice는 같은 CMS, Perspective는 기존 Substack RSS 동기화를 유지한다. 콘텐츠 저장은 웹사이트 소스 저장소를 변경하지 않는다.
- GitHub Pages artifact는 `tools/stage-site.mjs`로 방문자용 파일만 준비한다. 로컬 `board-content` 심볼릭 링크는 Git과 artifact에서 제외한다.
- 공개 출력 전용 `jsg-public-content` 저장소와 그 저장소로 제한된 배포 키를 이용한 자동 배포를 준비했다. 사용자 승인 후 출력 저장소·제한된 배포 키·Actions 비밀값 연결을 완료했다. Cafe24는 비활성 상태를 유지한다.

### Team·Portfolio 캐러셀 보완 — 2026-09-08

- 사용자의 추가 50% 가속 요청을 반영해 Team·Portfolio 자동 이동을 58.5px/초로 높였다. 두 목록은 같은 속도를 사용하며 두 목록은 `scripts/carousel.js`의 같은 네이티브 가로 스크롤 동작을 공유한다. 터치·휠·키보드 포커스는 자동 이동을 멈추며 재생·일시정지와 동작 줄이기를 유지한다.
- Partners의 모든 카드는 같은 높이를 사용한다. 새 상반신 사진이 없는 멤버도 기존 큰 인물 사진·이름·직함·보유한 경력과 상세 링크를 갖춘 완전한 카드로 표시한다. 사진·경력이 없는 경우에는 기존 이름·직함을 균형 있게 배치하며 내용을 새로 만들지 않는다. Advisors의 기존 표시 계약은 유지한다.
- Portfolio의 KOSDAQ·KOSPI 표시는 회사명 아래 오른쪽 끝에 둔다. 상자는 제거하고 JetBrains Mono를 사용하며 추가 요청으로 직전 크기보다 1pt 더 키웠다(기본 clamp 값 + 2pt). 회사명 행의 기존 높이는 유지한다.

### 공통 탐색 아이콘·디자인 유지 규칙 — 2026-09-08

- 모든 방향성 내비게이션·페이지 이동은 포함된 JetBrains Mono Nerd Font의 같은 네 방향 angle chevron을 사용한다. 왼쪽 U+F104(이전), 오른쪽 U+F105(다음·상세 진입), 위 U+F106(Top·섹션 복귀·접기), 아래 U+F107(펼치기)이다. 선이 달린 화살표, 대각선 화살표, 다른 글꼴의 방향 아이콘을 섞지 않는다.
- `styles/tokens.css`의 `--chevron-{left,right,up,down}`와 `--nav-chevron-size`(18px), `--nav-chevron-control-size`(20px)가 문자·크기를 소유한다. `styles/layout.css`의 `.nav-chevron[data-direction]`를 재사용한다. 방향 문자를 레이블에 직접 넣거나 개별 페이지에서 별도의 SVG·CSS 선 아이콘을 만들지 않는다.
- 사용 예: `<span class="nav-chevron" data-direction="right" aria-hidden="true"></span>`. 아이콘은 장식이며 링크의 실제 제목과 Previous/Next 접근성 이름을 유지한다. 아이콘 크기와 별개로 독립 버튼·복귀 링크는 최소 44px 조작 영역과 기존 포커스·비활성 동작을 유지한다.
- 적용 범위는 Notice·Perspective 페이지 버튼, Team·Portfolio·Notice 상세 이전/다음, 프로필 진입, Top·섹션 복귀와 모바일 메뉴 펼침/접힘이다. 모바일 메뉴는 닫혔을 때 아래, 열렸을 때 위 chevron을 표시한다. 내용 원문이나 문서 설명 속 방향 기호는 이 UI 규칙의 변경 대상이 아니다.
- Advisors 중 사진이 없는 6명은 기존 인물 사진과 동일한 회색 placeholder 영역을 유지한다. 데스크톱 108×140px, 모바일 80×110px의 기존 사진 열을 공유하며 이름·직함 열의 정렬을 보존한다. 가짜 사진이나 경력을 만들지 않는다.
- 앞으로의 수정은 이 규칙과 기존 팔레트·글꼴·카드·캐러셀 결정을 함께 따른다. 새로운 방향은 현재 사용자 승인 사항으로 기록하고, 겹치는 이전 규칙은 갱신한다. 검사 통과와 사용자 시각 승인은 구분한다.

### Portfolio CI — 2026-09-08

- 사용자 승인: 회사 상세 상단을 왼쪽 분야·회사명과 오른쪽 CI로 나눈다. CI는 테두리·배경 없이 원본 비율을 유지해 영역 높이에 맞추며 모바일에서도 두 영역을 유지한다. 소개 아래 공식 웹사이트 링크를 둔다. 홈 회사명 목록에는 CI를 추가하지 않는다.
- Pages CMS의 공식 웹사이트·기업 CI 필드가 콘텐츠를 소유한다. 흰색 CI는 선택적으로 검정으로 표시한다. 확인되지 않은 회사는 투명한 자리표시자로 CI 영역을 유지하며 공식 링크를 추정하지 않는다.
- 이번 변경은 로컬 구현·검증까지 진행한다. 사용자의 별도 지시 전에는 푸시·배포하지 않는다.
- 추가 사용자 결정: 홈 Team 제목 아래 한글·영문 설명을 제거한다.
- Team 제목의 한 줄 높이와 상하 여백은 Portfolio 제목과 같은 공통 값을 사용한다.
- 추가 사용자 승인: 콥틱은 운영 브랜드 브리즘(Breezm)의 공식 로고를 CI 영역에 사용한다.

### 스크롤 전환·전체 메뉴·원격 미리보기 — 2026-09-08

- 최신 사용자 승인: 제목의 sticky 느낌을 뚜렷하게 하고 이전 긴 버전의 절반 정도인 짧은 전환 공간을 허용한다. 홈 7개 섹션과 Team·Portfolio·Notice 상세에서 제목과 본문 시작을 같은 묶음으로 유지하며 스크롤 거리만큼 이동을 상쇄해 짧게 고정한다. Safari 브라우저 가장자리가 첫 sticky 색면으로 고정되는 현상을 피하기 위해 `position: sticky` 대신 `scripts/sectionReveal.js`의 제한된 translate를 사용한다. 고정 구간은 데스크톱 144px·모바일 96px이며 이후 일반 스크롤로 이어진다. 공간 높이는 고정되어 빠른 스크롤로 늘어나지 않는다. JavaScript 시간 지연이나 추가 flick 요구는 없으며 동작 줄이기에서는 고정·추가 공간을 모두 제거한다. 이 결정은 미세한 제목 parallax 실험을 대체한다.
- Perspective는 Philosophy 내부 콘텐츠로 유지하고 메뉴에만 Philosophy 바로 다음 항목으로 추가한다. 모든 화면에서 전체 메뉴는 햄버거로 여닫는다. Escape·바깥 클릭·섹션 선택으로 닫히며 현재 섹션과 키보드 접근성을 유지한다.
- 사용자 확인용 Tailscale 미리보기는 `https://jessie.adal-alhena.ts.net/jsg/`이다. `tools/preview-server.mjs`가 loopback 4174에서 현재 방문자 파일과 별도 콘텐츠 저장소의 `dist/`를 제공한다. URL 접두어는 응답에서만 바꾸며 기존 Tailscale 서비스 경로를 보존한다. 공개 GitHub Pages 배포·푸시는 별도 지시를 기다린다.
- 검증: 구조·동작 검사 33개와 자산 검사 PASS. 실제 모바일·데스크톱에서 7개 홈 전환과 3개 상세 전환, 본문 시작 보존·역방향·동작 줄이기를 확인했다. 내장 브라우저의 메뉴·직접 링크·Top·뒤로 가기는 PASS. 현재 Chrome 확장 환경에서는 Top·뒤로 가기 위치 차이가 재현되어 원인은 미확정이다. 공개 배포 전 후속 확인 대상으로 남긴다.


- Team 자동 이동 버튼은 Partners 제목 바로 오른쪽에, Portfolio 버튼은 Portfolio 섹션 제목 바로 오른쪽에 배치한다. Board 탐색과 같은 24px 간격과 44px 조작 영역을 사용하며 아이콘 대신 고정폭 글꼴의 일반 문자 `>`(재생)와 `‖`(일시정지)를 표시한다. 문자는 32px 폭 안에 가운데 정렬해 끝이 잘리지 않게 한다. 접근성 이름은 현재 가능한 재생/정지 동작을 알린다. 한글 버튼 문구와 목록 위의 별도 버튼 행은 제거했다.

- Perspective 글 링크는 새 탭으로 열어 JSG 화면과 탐색 위치를 유지한다. 링크 대상·제목·정렬과 기존 Substack 콘텐츠 소유자는 그대로 유지한다.

- 최신 검증 PASS: 1440px·390px의 7개 홈 섹션에서 제목 고정·해제와 역방향 스크롤을 확인했다. 섹션 높이가 일정하며 제목/본문 겹침과 가로 넘침이 없다. 모바일 3개 상세의 제목 고정도 확인했다. 재생/정지 아이콘 클릭과 제목 옆 정렬, 동작 줄이기에서 고정/추가 공간 제거 및 캐러셀 정지를 확인했다. Perspective 글 클릭은 실제 Chrome 새 탭을 열고 기존 JSG 탭을 유지했다. 구조·동작 33개와 자산 검사는 PASS다.

- 상세 페이지의 이전/다음 탐색은 가운데 정렬·최대 720px 폭과 위아래 16px 패딩을 사용한다. 링크의 44px 조작 영역은 유지한다. 상세 본문 컨테이너의 추가 하단 여백을 없애고 상세 전환 공간만 데스크톱 64px·모바일 48px로 줄여 탐색 아래의 중복 여백을 정리했다. 홈의 144px·96px 전환 공간은 유지한다.

- iPhone 최신 Safari 대응: 홈·3개 상세는 `viewport-fit=cover`로 화면 가장자리까지 사용한다. 헤더·펼침 메뉴·footer와 가로 여백에 safe-area inset을 반영한다. 제목 고정은 상단 inset 아래에서 시작한다. 기존 Cyan·Blue·Navy·White 팔레트는 그대로이며 별도 채도/투명도 보정이나 고정 색상 막대는 추가하지 않았다. PASS: 59px 상단·34px 하단 inset을 모의한 390px 브라우저에서 7개 섹션의 고정·겹침 없음·가로 폭, Partners 버튼 위치, 하단 여백 및 동작 줄이기를 확인했다. NOT RUN: 물리 iPhone의 Safari 브라우저 상·하단 색 표시. WebKit 근거: https://webkit.org/blog/7929/designing-websites-for-iphone-x/ 및 https://bugs.webkit.org/show_bug.cgi?id=301756 .

- 2026-09-08 사용자 승인: 현재 웹사이트 변경을 커밋하고 GitHub Pages에 게시한다. 공개 확인 대상은 https://taekimax.github.io/JSG_website/ 이며 iPhone Safari 고유 상·하단 표시의 물리 기기 확인은 별도 미검증 상태로 남긴다.
