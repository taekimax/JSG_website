export const LANDING_HERO_BUILD = '2026-04-03-svg-hero-03';
export const LANDING_HERO_BUILD_LABEL = 'svg-hero-03';

export function isLocalPreviewHost(hostname: string) {
  return hostname === '127.0.0.1' || hostname === 'localhost';
}
