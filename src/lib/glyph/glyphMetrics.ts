import type { GlyphId } from '@/types/hero';

export const glyphMetrics: Record<GlyphId, { width: number; height: number }> = {
  J: { width: 0.56, height: 1.0 },
  S: { width: 0.68, height: 1.0 },
  G: { width: 0.74, height: 1.0 },
};
